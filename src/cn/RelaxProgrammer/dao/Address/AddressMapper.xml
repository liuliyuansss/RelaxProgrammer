<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.RelaxProgrammer.dao.Address.AddressMapper">
<select id="CheckAddressByu" resultType="Address">
     select * from candy_address where user_code=#{user_code} order by user_time desc
    </select>
</mapper>